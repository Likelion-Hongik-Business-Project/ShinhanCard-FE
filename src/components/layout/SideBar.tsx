const SideBar = () => {
  return (
    <aside className="w-16 bg-white border-r flex flex-col items-center py-4 z-100">
      <nav className="flex flex-col items-center"></nav>
    </aside>
  );
};

export default SideBar;
